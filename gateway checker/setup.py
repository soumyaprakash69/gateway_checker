from setuptools import setup

setup(
    name="gateway_checker",
    version="1.0",
    install_requires=[
        "pyrogram>=2.0.0",
        "tgcrypto>=1.2.0",
        "requests>=2.25.0",
    ],
)